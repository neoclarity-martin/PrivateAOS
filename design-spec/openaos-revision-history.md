---
title: OpenAOS Design Specification — Revision History
file_type: design_spec
project: OpenAOS
openaos_version: 3.2.0
created_date: 2026-06-02
last_updated: 2026-08-12
status: design_ready_for_generation
---

# OpenAOS Revision History

Part of the OpenAOS Design Specification document set. The canonical design is in `openaos-design-spec.md`; this file holds its dated revision and consistency-resolution history. The specification remains the single source of truth (Section 1.6.1).

Entries below are in reverse chronological order (newest first). Rows are
historical records and keep the names in use at the time they were written
(2.x rows use `spec_version`, `aos-factory`, agent-era filenames); the version
column is the single version track, named `openaos_version` as of the 3.0
Phase C rename sweep.

| openaos_version | Date       | Change                                                       |
| ------------ | ---------- | ------------------------------------------------------------ |
| 3.2.0        | 2026-08-12 | **§36.2 Plugin Generation — closes the round trip left open by the 2026-08-05 and 2026-08-12 §36.3 runs.** Regeneration was a near no-op for the two mechanical buckets: `content/` and `workflow-specs/` were already byte-identical, and the three skills matched their recorded baseline, so the §36.2 step 2 baseline guard passed and no unreflected plugin edit was at risk of being clobbered. The substantive work was the **branding sweep Phase B deliberately deferred** (issue 3): the 12 `design-spec/content/` sources and their `claude-plugin/openaos/content/` mirrors were capitalized **in one change** so VC2/VC13 byte-identity never broke mid-phase — 25 display-name substitutions across 11 file pairs (root `CLAUDE.md`/`AGENTS.md`, `governance.md`, `feedback.md`, the §16.6 user-guide template, and the seven §18.2 report templates, whose provenance comments as well as footers carried the lowercase form). The §1.1 exclusion rule held throughout: `setup-openaos`, `openaos@neoclarity.ai`, `openaos_version`, `name: openaos`, and the `claude-plugin/openaos/` path are untouched, and the one lowercase occurrence remaining in the plugin README is the slug inside a code span. **Version stamps corrected.** The seven report-template footers printed `openaos 3.1.0` into every generated user report, and their frontmatter comments carried the same stale value; both moved to 3.2.0, consistent with the last-changed stamping convention the content tree already follows (the three `.md` interaction templates stay at 3.0.0 — they did not change). The plugin `README.md` provenance line, likewise stranded at 3.1.0, now reads 3.2.0. **`file-skeletons.yaml`:** only the user-guide `sections:` list moved (`What openaos Is` → `What OpenAOS Is`), because VC9 compares `<h2>` text case-sensitively and would have failed otherwise; the `root_claude_md` `required_anchors` entry was left lowercase, since VC10 lowercases both sides before testing containment and the value is a matcher token, not display text. B2's handoff assumed both would move. **Skills:** two prose occurrences ("the openaos builder engine", "the openaos refinement engine") were capitalized in `build-workflow` and `refine-workflow` — spec → plugin, the sanctioned §36.2 direction, and the baseline was re-recorded afterward. **Invocation phrase** `set up openaos` → `set up OpenAOS` in both READMEs: spec line 326 makes workflow invocation intent-matched with `Proceed` the only exact string, so the phrase is illustrative prose, not a command, and the skill's own heading already read "Set Up OpenAOS". All six validators green; `sync-state.py check` clean against the re-recorded 28-pair baseline; §28.1 layout still exactly 30 files. **No `openaos_version` increment** — the packaged framework's behavior is unchanged. Deliberately **out of scope**, and logged rather than swept: repo-root `README.md` and `CONTRIBUTING.md` were never in Phase B's sweep set and still mix both forms, and the root README's `design-spec/` tree understates the document set the same way §34 item 4's Document Set did before B4 — both go to a reviewed root-docs pass, not into a generation step. Two questions raised for §36.1 Phase D: §14.2 says a file records the version it was *rendered from*, which the content tree's last-changed convention contradicts, and VC10's case-insensitive substring test means the `openaos` anchor is also satisfied by `setup-openaos`, so it would pass even if every prose mention were deleted. |
| 3.2.0        | 2026-08-12 | **Spec sync from plugin (§36.3) — `skills/setup-openaos/SKILL.md`, `skills/build-workflow/SKILL.md`.** Resolves issues 1 and 2 of the 2026-08-12 §36.1 review. Origin commits `4d613cd` and `8ea4a0c` had edited the setup skill directly with no spec change, and `sync-state.py check` reported 28/28 because the baseline was recorded *after* those edits — the same masking failure the 2026-08-05 row documents for `content/root/AGENTS.md`, and the second time it has hidden real drift. **Reflected into the spec:** the welcome acknowledgment gate (§8.1 step 1 — recorded explicitly as *distinct from* the §3.1 write gate, which continues to govern the scaffold, so the design now has one gate on starting the interview and one on writing files, and neither weakens the other); the long-form welcome text naming what an Agentic Operating System is, now canonical in `setup-interview.md`; and three skill steps that had **no spec counterpart at all** and which the review's own issue list missed — rhythm scheduling with its four default cadence times (weekday 04:00 / 16:30, Friday 12:00, first Tuesday 12:00, recorded as defaults the user may change, creating no definition file and so not engaging the §14.8 drift invariant), user-guide generation and display (§8.1 step 6, listing what was actually installed rather than the full catalog), and the scripted close (§8.1 step 7). The string "schedul" had appeared nowhere in the specification before this cycle. **`work-focus` removed** per user direction, with its two orphaned captures explicitly rewired rather than dropped: use-case menu ordering falls back to `workflow-catalog.yaml` order, and the `/memory/user-profile.md` seed is built from `memory-seeds` plus the new `work-platform` answer. **Use-case label renamed** "Organizer / Declutter" → **"File organizer"** across all 13 tracked occurrences, adopting the plugin's wording per user direction: the Frame's five-use-case list (the normative paragraph tenet 1 cites), the §7B table, `workflow-catalog.yaml` `display_name`, both halves of the `workflow-specs/organizer/spec.md` byte-identical pair (WS1–WS2), both halves of the `organizer-report-template.html` pair (VC2, `html_report_organizer`), both READMEs, `setup-interview.md`, and the `build-workflow` skill description. The **slug stays `organizer`** — renaming it would cascade into `artifacts_owned`, the `file-organization` domain, the `workflow-specs/` directory, and the template filename, for no user-visible gain. **The connectors question, and a tenet amendment drafted then withdrawn.** The shipped skill had gained a question asking which connectors to use (Google Workspace / Microsoft 365 / Dropbox / connect later). The §36.3 step 2 tenet check first read this as a **tenet 1 (scope discipline)** failure — a connector concept the 3.0 Minimal design does not have — and the user directed `accepted-as-spec-change`, so an amendment to runbook §33.1 was drafted admitting a platform-preference memory seed. **It was withdrawn on review**, for three reasons worth recording. (1) It violated §33.1's own construction rule: tenets are *"drawn from what is already normative elsewhere in the design, restated here"*, and the amendment put original design content into the restatement, leaving the tenet saying something its cited authority — the Frame's removed-concepts paragraph — does not. (2) It asserted a consumer that did not exist: the §20.1 prose claimed generated workflows read the seed, but nothing in the design reads `/memory/user-profile.md` at all; the §7B.2 justification rested on a mechanism that was never specified, which is the same defect as the original question capturing nothing, better disguised. (3) It forbade the mechanism delivering its own value — banning "per-platform code paths in `build-workflow`" while the seed's entire purpose was for build-workflow to use the platform in generated wording, a line too blurry for the next maintainer to apply at a §36.3 check. **Re-diagnosed and redesigned:** the question never failed tenet 1. `memory-seeds` already asks about "people, **tools**, or preferences", so capturing which platform a user works in was always in scope; the shipped question failed **§7B.2** as a pre-optioned duplicate that captured nothing. Resolved through spec change instead — **§20.1** defines the work-platform seed as one instance of the memory capture already performed, **§12.1(a)** names its single consumer and constrains it to step *wording* (selects no branch, changes no step sequence, never assumes a live connection), and the question is recast as `work-platform`, dropping the "I'll connect later" option that presumed a connection flow the design does not have. **Tenet 1 is unchanged**, and `accepted-as-spec-change` remains unused — no precedent that a plugin edit failing a tenet can be legitimized by amending the tenet it failed. Left open deliberately: whether OpenAOS acknowledges host-provided connectors at all is a real capability question and is logged for §36.1 Phase D rather than settled by implication through a setup question. **Issue 2 fixed:** the setup skill instructed the agent to stamp generated files with `` `OpenAOS_version` ``, collateral damage from `8ea4a0c`'s branding sweep catching a code-span identifier; corrected to the controlled `openaos_version` (§14.2, §15.3). This defect was invisible to every validator because it lived in *generated user output* rather than a tracked artifact — every file a real setup run created would have carried a non-conforming frontmatter key. Zero occurrences of the old string remain repo-wide. `build-workflow/SKILL.md` was touched only by the label rename, and its use-case names derive from the Frame and §7B rather than from §12, so the rename is a reflection rather than an unreflected patch. Logged at 3.2.0, **no increment** — §36.3 proposes no bump (step 8) and the user directed none during the preview. The sync-state baseline was re-recorded *after* this reflection, not before — refreshing it early is what masked the drift twice. Open: §36.2 regeneration to close the round trip (step 9), still open from the 2026-08-05 run; re-run of §36.1 steps 2–4; a new issue recording that the review missed the steps 5–7 scheduling/user-guide/close drift; `openaos@neoclarity.ai` mailbox verification (§34, §37). |
| 3.2.0        | 2026-08-05 | **Spec sync from plugin (§36.3) — `content/root/AGENTS.md`.** The first §36.3 run, closing the `content/root/AGENTS.md` drift left open by the row below. Scope detection found one changed file in the mechanical bucket and none in the authored bucket (`sync-state.py status` 28/28 against baseline; `validate-skills.py` SK1–SK5 green), and the change was a single character: a trailing colon added to the `Import these two files at the start of every session` lead-in on the plugin side. Tenet check (§33.1) passed on all five — punctuation on a lead-in sentence touches no scope boundary, removes no part of the governance layer, does not move the skills/workflows refinability line, introduces no unsanctioned write path, and adds no artifact type — so nothing was excluded and `sync-exclusions.md` gained no row. `sync-plugin-to-spec.py --apply` copied the plugin file onto `design-spec/content/root/AGENTS.md`; the two trees now diff empty and **VC2 is green for the first time since the 3.2.0 router cycle**. No §16.10 follow-on was needed: the section's contract prose specifies the required anchors and the relative-import rule, neither of which this punctuation affects, and the file continues to satisfy VC10's four `required_anchors` and VC13. Worth recording that the drift was invisible to every validator except the byte-identity check — `sync-state.py` reported 28/28 because the baseline had been recorded after the plugin-side edit, so the byte comparison in §36.3 step 1 was the only thing that caught it; this is the case for keeping VC2 and WS1–WS2 as CI gates rather than relying on the local state file, which §36.3's own design already assumes ("correctness never depends on it"). Validators after the write: `validate-content.py` VC1–VC13 clean, `validate-workflow-specs.py` WS1–WS2 at 5 sources, `validate-skills.py` SK1–SK5, catalog 0/0 at 10 entries, vocabulary VV1/VV2/VV4/VV6, version 12 files at 3.2.0 — all green; baseline re-recorded. Logged at 3.2.0, **no increment** — §36.3 restores equality and never changes the packaged framework (runbook §36.3 step 8), and the user directed no bump during the preview. Open: §36.2 regeneration to close the round trip (§36.3 step 9), which is expected to be a zero-diff pass for this file; `openaos@neoclarity.ai` mailbox verification (§34, §37). |
| 3.2.0        | 2026-08-05 | **Bidirectional spec/plugin sync — new runbook §36.3, tenet gate, and CI byte-identity enforcement (issue 12).** Full plugin generation was non-deterministic because one §35 source class — the three `skills/*/SKILL.md` — is *authored* from §8.1/§12/§13 each pass rather than copied, so changes had increasingly been made directly to the plugin instead of the spec, producing real drift (`content/root/AGENTS.md` still differs between the two trees). This cycle sanctions the return path without weakening the spec-first claim. **Two one-way flows in a fixed order, not a symmetric mirror:** new **§33 ordering rule** states that §36.3 (plugin → spec) always runs before §36.2 (spec → plugin) within a cycle, so the two directions are never concurrently live and there is no conflict-resolution logic to write. New **§33.1 Fundamental Design Tenets** restates five already-normative constraints (scope discipline, non-removable governance layer, skills-vs-workflows boundary, drift invariant, single-artifact distribution) as the gate every plugin → spec reflection passes; a violating change is not reflected, and is reported and logged rather than silently dropped. New **§36.3 Spec Sync (Plugin → Spec)**, triggered by "Using the runbook, sync the design spec with the plugin.", runs nine steps: two-bucket scope detection (mechanical `content/*`/`workflow-specs/*` by byte comparison, authored `skills/*.md` against §8.1/§12/§13), tenet check, mechanical copy, authored reflection *drafted for the author to review and own*, combined preview, one `Proceed` gate for the batch, validation, a revision-history row, and a hand-off back to §36.2. The four mechanical validators run at the **end only** — a precondition run would block syncing the very drift that broke them — and §36.3 **never** proposes an `openaos_version` bump, since it restores equality rather than changing the packaged framework. **§36.2 gained a baseline guard** rather than the first draft's blanket refusal to regenerate existing skill files (which would have stranded the spec, leaving no route for a §8.1/§12/§13 edit to reach the plugin): each `skills/*.md` is compared against the recorded sync baseline — unchanged, regenerate normally; changed, stop and name the file and direct the user to §36.3 first. **§35** now documents direction and authority per bucket, and **§34** gained three checklist items (workflow-specs byte-identity, no unreflected skill edits, no undecided exclusion). New tooling: `scripts/syncpairs.py` (single shared definition of the spec↔plugin pair map, so the three consumers cannot disagree), `scripts/validate-workflow-specs.py` (**WS1–WS2** — the workflow-specs byte-identity rule §36.2 step 4 previously stated in prose only, now the mechanical sibling of `validate-content.py` VC2), `scripts/sync-state.py` (status/check/record over `.openaos-sync-state.json`), and `scripts/sync-plugin-to-spec.py` (preview-by-default mechanical copy with `--exclude` for tenet-check exclusions). **Sync state records both hashes of every pair** — a plugin-side hash alone cannot tell whether the spec side was actually updated — and is **local, untracked (gitignored), and recomputable**: it changes every run so tracking it would conflict on nearly every merge, each contributor keeps their own, and a fresh clone simply compares in full (noisy once, never wrong). Correctness never depends on it. New tracked **`design-spec/sync-exclusions.md`** records tenet-check exclusions with date, file, tenet, attempt, and disposition; it is what the new §34 item reads (placed under `design-spec/` rather than the manifest's `internal-only/` path, which is gitignored and so could not have been tracked). **CI** gained a `plugin-matches-spec` job running `validate-content.py` and `validate-workflow-specs.py`, with path triggers widened to `claude-plugin/**` — the byte-identity invariant is exactly what makes an unreflected plugin edit unable to reach `main`, so a branch skipping the round trip fails automatically. The tenet check is deliberately **not** mechanized into CI: it is a judgment call and should not pretend to be a script. **`CONTRIBUTING.md`** documents §36.3 as an optional drafting aid (prototype in `skills/`, then sync, then *review and edit the draft — you author the spec language you submit*), and "What not to do" was corrected from "do not modify the generated plugin" (which flatly contradicted the new step) to "do not *submit* direct modifications"; the submission contract — only spec pull requests, the spec wins, governance layer not removable — is unchanged. One spec correction: the Frame's removed-concepts paragraph cited `internal-only/feature-specs/3.0-removal-manifest.md`, which moved to `implemented/`; path corrected rather than the sentence deleted, preserving the enumeration tenet 1 cites as non-normative detail. Both new checks negative-tested (a one-byte plugin edit fires WS2 and the §36.2 guard respectively; reverting returns both to green). Validators: catalog 0/0 at 10 entries, vocabulary VV1/VV2/VV4/VV6, version 12 files at 3.2.0, workflow-specs WS1–WS2 at 5 sources, all green; `validate-content.py` **VC2 fails on the pre-existing `content/root/AGENTS.md` drift**, which is precisely the drift §36.3 exists to resolve and is left for a `Proceed`-gated §36.3 run rather than folded into this change. **Both open items from the manifest were resolved in the same cycle rather than deferred.** *(1) Depth of the CI check on `skills/*.md`* — settled at **anchors, not prose**, implemented as new `scripts/validate-skills.py` (**SK1–SK5**): exactly the three plugin-owned skills present; frontmatter `name` equal to the directory name and `description` non-empty; every `§N` citation resolving to a real section of the spec set *and* each skill citing its governing section (setup-openaos §8, build-workflow §12, refine-workflow §13); every in-plugin `content/`/`workflow-specs/` reference resolving, with placeholder paths (`workflow-specs/[slug]/spec.md`) required to match at least one real file and the §16.10 `content/X → /X` mapping prose exempted as the documented non-path match §36.2 step 4 already excuses by hand; and the literal Proceed gate present in each skill. The deeper option — `skill_*` skeletons in `file-skeletons.yaml` enforcing heading sets the way VC4 does for content — was **rejected**: it would add a third place to keep in sync and, more importantly, would freeze the authored wording of the very drafting surface §36.3 exists to support. Prose fidelity to §8.1/§12/§13 stays a review responsibility (§36.1, §36.3 step 4), which is the honest boundary — the same reasoning that keeps the tenet check out of CI. This closes the last hand-checked item in §36.2 step 4, which now names the script instead of listing checks in prose. *(2) Undecided exclusions* — settled as a **finding on the §36.1 step 4 issue list**, not a separate hard-fail and not information-only. That list already gates finalization, so an `open` disposition stops a review from *finalizing* without stopping it from *running*, and it clears by recording any disposition including an explicit decision not to act. Rationale recorded in §34, §36.1 step 4, §36.3 step 2, and `sync-exclusions.md` itself: an unresolved conflict between a plugin-side change and a fundamental design tenet is a decision the project owes itself, but blocking an unrelated review outright would punish the wrong cycle. All five SK checks negative-tested (a bogus citation fires SK3 twice — unresolvable id and missing governing section; a bad directory reference fires SK4; replacing the Proceed literal fires SK5; the file restores to green). Logged at 3.2.0, **no increment** — runbook procedure, contributor docs, and tooling only; no generated artifact changed. Open: the §36.3 run to resolve the `content/root/AGENTS.md` drift; `openaos@neoclarity.ai` mailbox verification (§34, §37). |
| 3.2.0        | 2026-08-04 | **Root-scaffold import form made normative and checked — §16.10 relative-import rule, new VC13.** A router-pattern audit found the reference workspace `OpenAOS 260717.1` importing `@/governance/governance.md` and `@/governance/workflow-router.md` from its `/AGENTS.md`. Per the documented `@path` resolution rule, an import resolves relative to the file containing it and a leading slash makes it absolute against the *filesystem* root, so both imports had been resolving to a non-existent `C:\governance\...` and silently loading nothing since 2026-07-18 — the workspace still functioned because agents read those files on demand once oriented, which is precisely what makes the failure mode worth a mechanical guard. The workspace's two imports were corrected to the relative form (data-file fix in a live workspace; no design artifact affected). **§16.10** gained a normative paragraph requiring the relative form in every root-scaffold `@path` import, with the silent-failure rationale, and **§18.7** gained a *Root scaffolds specifically* criteria block. `validate-content.py` gained **VC13**: no `@/` absolute import in any `root_scaffold:` content source, and every relative import resolving to a real sibling scaffold or content source (code spans and fenced blocks stripped before scanning, since those are not imports). Verified by negative test — reintroducing `@/governance/workflow-router.md` fires VC13, and removing it returns the tree to green. `content/root/AGENTS.md` already used the correct relative form, so **no content source or plugin file changed** and byte-identity holds; VC1–VC13 green at 20 sources, with catalog, vocabulary, and version validators unchanged and green. Logged at 3.2.0, **no increment** — spec prose and validator only, no change to any generated artifact. Open: whether a new normative rule plus validator warrants a 3.2.1 patch increment on its own (see note below); `openaos@neoclarity.ai` mailbox verification (§34, §37). |
| 3.2.0        | 2026-08-02 | **§36.2 Plugin Generation — workflow-router pattern packaged.** Renders the 3.2.0 design into `claude-plugin/openaos/`. `content/` gained the new **`governance/workflow-router.md`** (20 content sources, up from 19) and refreshed copies of `governance/governance.md`, `root/AGENTS.md`, and all five `workflows/*.md` (the `When to Use` removal plus 3.2.0 stamps); all five `workflow-specs/*/spec.md` refreshed for their updated `captures:` fields. The three `skills/*/SKILL.md` were authored from §8.1/§12.2/§13.2: `setup-openaos` scaffolds and previews the router and restates the governance layer as everything under `/governance/`; `build-workflow` gained a **Routing** section (elicit two-to-four natural phrasings during Propose, show the row in Preview, append-only on `Proceed`, never edit or reorder an existing row) and dropped `When to Use` from its workflow shape; `refine-workflow` gained the trigger-in-the-router rule and a cue for recognizing friction that is a routing problem rather than a workflow-body one. `plugin.json` bumped to 3.2.0. **Step 4 validation all green:** `content/` and `workflow-specs/` byte-identical to their design-spec sources (empty diff both trees), `validate-content.py` VC1–VC12 clean at 20 sources, `plugin.json` well-formed at `name: openaos` / `version: 3.2.0`, `.claude-plugin/marketplace.json` `source` = `./claude-plugin/openaos`, the three SKILL.md files carrying `name`+`description`, in-skill `content/` references resolving (the sole non-path match being the §16.10 `content/X → /X` mapping prose), report-producing workflows still pointing at real templates and `/outputs/[slug]-<date>.html` paths, and no live `status-report-template.md` reference. Logged at 3.2.0, **no increment** (the packaged change is the 3.2.0 design already incremented by the row below). Housekeeping: the stale `claude-plugin/openaos-3.1.0.zip` packaging artifact (pre-router — its `AGENTS.md` imported a router that did not exist in the archive) was replaced by `openaos-3.2.0.zip`, rebuilt from the regenerated tree at 30 files under a single `openaos/` root, integrity-checked, and carrying `plugin.json` at 3.2.0; both are untracked and gitignored (`*.zip`), so no history change. Open: `openaos@neoclarity.ai` mailbox verification (§34, §37). |
| 3.2.0        | 2026-08-02 | **Workflow-router pattern adopted — routing decoupled from workflow bodies.** An agent previously had to read workflow files to learn when each applies; routing now costs one small file loaded at session start. New **`/governance/workflow-router.md`** (new `router` file_type, new **§16.11** schema, new `workflow_router` skeleton): a two-column markdown table — `Example Prompts` \| `File to Load` — chosen over YAML/JSON because the data is flat and uniform and a table carries the least per-row overhead in a file loaded into every session. Its Purpose section states the matching rule for the agent reading it (prompts are examples; match on meaning; load exactly one workflow; ask when nothing matches). Setup ships it with the five §17 governance rows (§8.1, §6, `setup-openaos/SKILL.md`). **§16.10** and `content/root/AGENTS.md` gained a *Workflow routing* section importing `@governance/workflow-router.md`, so the router loads through the existing `/CLAUDE.md` → `/AGENTS.md` chain. **§16.3 drops `When to Use`** from the workflow schema (skeleton, all five `content/workflows/*.md`, and the five `workflow-specs/*/spec.md` `captures:` fields updated) — trigger authority moves wholly into the router, eliminating the two-places-to-sync problem that made the old section drift-prone. **§12.2** grants build-workflow one narrow governance-file write: append the new workflow's row on the same `Proceed` that writes the workflow — append-only, never editing an existing row, justified because a workflow without a row is unreachable, so the row is part of creating it (§27 now treats a rowless build as incomplete). **§13.2** requires refine-workflow to update the row when triggers change and to show it in the same previewed diff. **§14.8** records the router as a definition file and the widened writer set as a bounded change to the drift invariant, not an exception. The **non-removable governance layer is restated as everything under `/governance/`** (§16.1, §14.8, AGENTS.md, governance.md, `refine-workflow/SKILL.md`) so the router cannot be removed, which would leave every workflow unreachable. **§18.7** gained router criteria and `validate-content.py` a new **VC12** (rows resolve to real workflows; every workflow has exactly one row; no workflow file carries `When to Use`); §27, §28.1, and runbook §34/§35 updated. Change 6 of the request — governance.md `Memory`/`Workflows`/`Logs` section titles — was already in place from the 3.1.0 cycle; the Workflows section gained the router-ownership rule. Validators: catalog, vocabulary, and version green at 3.2.0; `validate-content.py` VC1/VC3–VC12 green, with VC2 (plugin byte-identity) failing by design pending §36.2 regeneration. **Incremented 3.1.0 → 3.2.0.** Open: plugin regeneration (§36.2), `openaos@neoclarity.ai` mailbox verification. |
| 3.1.0        | 2026-07-22 | **§36.2 Plugin Generation — zero-diff pass; schema `$id`s reconciled.** Closes the item carried forward by the §36.1 review below. The `$id` of `design-spec/vocabulary.schema.json` and `design-spec/workflow-catalog.schema.json` still named the pre-3.0 repo (`neoClarity-AI/Open-AOS-Factory`, the factory concept 3.0 removed); both updated to `neoClarity-AI/OpenAOS`, matching the §1.6.10 project URL. Org and path segments unchanged — the repo segment only. Neither schema is shipped in the plugin (§28.1 does not list them; they are design-time CI artifacts) and both validators load them by filesystem path rather than dereferencing the `$id`, so this is correctness-of-record with no runtime or packaging effect; both files re-confirmed well-formed JSON and still bound to their validators (VV1 and the catalog shape checks pass). **Generation itself produced no diff:** none of the four §35 source classes changed in this cycle or the §36.1 cycle preceding it — the three `skills/*/SKILL.md` render from §8.1/§12/§13 (untouched), `workflow-specs/` and the whole of `content/` are copies that already diffed empty, `plugin.json` holds at 3.1.0 (no increment), and the `README.md` URL line was applied during the §36.1 cycle. Rather than rewrite 29 files with their own bytes, the generation ran as its §36.2 step 4 validation pass, all checks green: `content/` and `workflow-specs/` byte-identical to their design-spec sources, `plugin.json` well-formed at `name: openaos` / `version: 3.1.0`, `.claude-plugin/marketplace.json` `source` = `./claude-plugin/openaos`, the three SKILL.md files carrying `name`+`description`, in-skill `content/` references resolving, HTML report templates self-contained (no external stylesheet/script/font/image, no `@import`), and the only surviving `status-report-template.md` reference being the exempt governance Change Notes record of its retirement. Housekeeping: the stale `claude-plugin/openaos-3.0.0.zip` packaging artifact (3.0.0-era, still containing the retired `status-report-template.md`) was deleted — untracked and gitignored, so no history change; `claude-plugin/` now matches the §28.1 layout exactly. All four validators green. Logged at 3.1.0, **no increment** (packaged plugin unchanged). Open at release: `openaos@neoclarity.ai` mailbox verification (§34, §37). |
| 3.1.0        | 2026-07-22 | **§36.1 Design Readiness Review (full mode)** — the first readiness review against the 3.x design (the previous cycle, 2.4.1, predates the 3.0 rewrite), so all §34 items were verified from scratch rather than incrementally. Mechanical checks green at every step (`validate-workflow-catalog.py` 0/0, 10 entries / 10 domains; `validate-vocabulary.py` VV1/VV2/VV4/VV6; `check-spec-version.py` 12 files agree at 3.1.0; `validate-content.py` VC1–VC11, 19 sources byte-identical to the plugin), plus by-hand verification that `workflow-specs/` diffs empty against the plugin and all five specs carry the §7B.1 section order, `plugin.json` is well-formed at `name: openaos` / `version: 3.1.0`, the marketplace `source` resolves, the three SKILL.md files carry `name`+`description`, in-skill `content/` references resolve, and no live `status-report-template.md` reference survives. **Five findings resolved:** (1) §16.7's feedback-log `Status` enum omitted `captured` — the value §17.5 step 1 and `content/workflows/feedback.md` actually assign — while enumerating an unreachable `approved` that no step or rule sets; enum corrected to `captured \| staged \| sent \| discarded`. (2) Two conflicting `/outputs` naming rules: §4 and §29 mandated date-first `YYYY-MM-DD-[slug].md` while §18.2, §17.1–§17.4, §12.5, and §16.1 all mandate slug-first `[slug]-<date>.html` — the form every workflow emits and `validate-content.py` enforces; slug-first made canonical in §4 and §29, with inbox-triage's `<run>` suffix left as the §18.2-documented local exception. (3) Spec §168 named "§36.2 OpenAOS Generation, or §36.3 Claude Plugin Generation" — 2.x naming for a split the 3.0 runbook collapsed into one workflow; corrected to §36.1 and §36.2 Plugin Generation. (4) The §1.6.10 project URL and the **user-facing** `claude-plugin/openaos/README.md` shipped `neoClarity-AI/Open-AOS-Factory`, naming the factory concept 3.0 removed; both updated to `https://github.com/neoClarity-AI/OpenAOS` (destination repo, not the working fork). (5) §17.1 described the daily-startup brief as "the four `brief_categories`" while `html_report_daily_startup` and the shipped template define six (the four inbox categories plus "What matters today" and "Completion"); prose corrected to describe all six and to cite the skeleton as the ordered list — no data-file or template change, the two already agreed. **Carried forward:** the `$id` of `vocabulary.schema.json` and `workflow-catalog.schema.json` still carries the old repo name — a schema-identity change deliberately deferred to the next §36.2 Plugin Generation rather than folded into a review cycle; and `openaos@neoclarity.ai` mailbox verification remains the open §34/§37 release item. All changes were prose- or single-line-scoped, each `Proceed`-gated individually; `design-spec/content/` stayed byte-identical to the plugin throughout. Logged at 3.1.0, **no increment** (per user instruction). |
| 3.1.0        | 2026-07-22 | **Content sources made deterministic, hand-editable, and validated (issue 2).** The plugin's ship-verbatim files now have a canonical source under the spec: new **`design-spec/content/`** mirrors the plugin's `content/` exactly (19 files — `governance/governance.md`, the five `workflows/*.md`, the seven §18.2 HTML report templates + the §16.6 user-guide template + the three §18.3/§18.5/§18.6 interaction templates under `templates/`, and the two §16.10 root scaffolds under `root/`), so spec→plugin is a **byte-identical copy**, not a per-pass re-render — the same model already used for `workflow-specs/`. The plugin's `templates/{CLAUDE.md,AGENTS.md}` moved to **`content/root/`** and the `templates/` folder was removed; §16.10 and `setup-openaos/SKILL.md` record the one deliberate exception to the `content/X → /X` mapping (`content/root/` ships to the workspace root). §16.1, §16.3, §16.6, §17, §18.2, §18.3, §18.5, §18.6, and §16.10 are **kept as contracts** and each gained a canonical-body pointer; no section deleted. New **§18.7 Content Source Validation** states normative, mechanical criteria (frontmatter keys/vocabulary/version-not-ahead, `##` section presence/order/non-emptiness against `file-skeletons.yaml`, required in-section markers, governance action lists equal to `vocabulary.yaml`, workflow Outputs resolving to a real template and `/outputs/` path, HTML well-formedness + §18.1 CSS embedding + self-containment + card-section order and class set, root-scaffold anchors, tree completeness and byte-identity), implemented by the new **`scripts/validate-content.py`** (VC1–VC11) and wired into §27, §34, runbook §35/§36.1/§36.2. `file-skeletons.yaml` gained explicit skeletons for the three interaction templates and the user guide, a `required_markers` map on `governance_config`, a `root_scaffold:` anchor list, and an updated scope note. One consistency fix surfaced by the new validator: governance.md's Level 1 list read "read files within its approved scope" against `vocabulary.yaml`'s "Read files within their approved scope". All four validators green; `content/ ↔ design-spec/content/` diff empty; `plugin.json` unchanged. Logged at 3.1.0, **no increment** (per user instruction). |
| 3.1.0        | 2026-07-22 | **Output reports standardized as HTML.** Every recurring, structured, user-facing workflow report (governance four — daily-startup, end-of-day, weekly-review, monthly-review — plus the inbox-triage, organizer, and learning-assistant use cases) now renders as a self-contained, card-based, color-coded HTML file instead of markdown, retiring the 2.x `status-report-template.md`. §18 restructured: new §18.1 Canonical Report CSS (one CSS block embedded verbatim by every template — a style change touches one place) and §18.2 HTML Report Templates (the seven templates, their card-order/section mapping, and generation rules); §18.3/§18.5/§18.6 (decision-entry, approval-request, memory-entry) unchanged and kept as markdown interaction templates. §17.1–§17.4 Outputs/Steps updated to render via their template to `/outputs/[slug]-<date>.html`. §16.1 governance config schema gained the shipped-by-default "Output reports render as HTML" Local Rule, renamed Memory Boundaries → Memory, and added Workflows/Logs sections (parity with the standing-rules coverage the section claims). §8.1 setup flow ships the governance four templates at setup; new **§12.5 Report-Output Scaffolding** defines the condition (recurring/structured/user-facing → scaffold; free-form or user-chosen shape → don't) that build-workflow applies for design-new workflows and that decided the three use-case templates (research-assistant excluded — output form is user-chosen per run; writing-assistant excluded — prose, not a report). New **§13.4 Batch Refinement Mode** in refine-workflow: an upfront-elicitation, one-consolidated-diff, single-`Proceed` exception to the one-workflow-per-session default, for cross-cutting policy changes touching many workflows plus governance.md at once (the change this row itself required). `file-skeletons.yaml` gained the §18.1 canonical-CSS entry and seven `html_report_*` skeletons (retiring `status_report`), and the `governance_config` skeleton's section list was updated to match. Runbook §35/§36.2 updated (template render mapping; validation now checks for stale `status-report-template.md` references and that every report path resolves). Plugin regenerated: `claude-plugin/openaos/content/templates/` gained the seven `.html` templates and dropped `status-report-template.md`; `content/governance/governance.md`, the four `content/workflows/*.md` files, and `workflow-specs/{inbox-triage,organizer,learning-assistant}/spec.md` updated to match; `research-assistant/spec.md` gained a Notes clarification; `.claude-plugin/plugin.json` bumped. Validators (`validate-workflow-catalog.py`, `validate-vocabulary.py`, `check-spec-version.py`) green. **Incremented 3.0.0 → 3.1.0.** |
| 3.0.0        | 2026-07-17 | **Complete Minimal, workflow-first rewrite (3.0)** — the 2.x AOS Factory design (preserved at tag `spec-v2.4.2`) replaced end to end per feature spec `3.0-create-minimal-openaos.md`, Phases A–H. Agents → workflows; aos-factory → OpenAOS (file set renamed; `spec_version` → single `openaos_version`, secondary `catalog_version`/`vocabulary_version`/`skeletons_version` tracks removed). Deleted: the 15-agent roster, Pro tier, DDD relationship vocabulary/typed edges, factory/instance split. Authored fresh: §16.1 governance config schema + §17 five governance workflows (governance coverage table confirms no load-bearing 2.x Security/Memory/Review/Feedback rule dropped); §7B five use-case builder specs (`workflow-specs/[slug]/spec.md`, new `workflow_spec` file_type); §12 build-workflow engine (one engine, two modes: instantiate / design-new, with the general AI-pattern library); §13 refine-workflow engine + §14.8 drift-invariant amendment; §8/§14.4/§16.6/§16.10/§28 plugin-only distribution. Runbook rewritten as the packaging runbook (§33–§37); plugin regenerated at `claude-plugin/openaos/` (3 skills, rendered governance + workflows + templates, byte-identical workflow-specs); marketplace source updated. Validators (`validate-workflow-catalog.py`, `validate-vocabulary.py`, `check-spec-version.py`) green at every phase commit. Open at release: `openaos@neoclarity.ai` mailbox verification (runbook §37). **Incremented 2.4.2 → 3.0.0.** |
| 2.4.2        | 2026-07-15 | Closed two mechanical-enforcement gaps surfaced by the 2.4.1 §36.1 review. **Gap 1:** `scripts/validate-catalog.py` now implements the two §7A.5 checks it previously only *required* — V5 reciprocity matched by **trigger token** (promoted from a direction-only warning to an error) and **V14** (no dead relationship/edge-sourced-trigger tokens, reading the §7A.6 carve-out from the catalog's `# edge-sourced` marker). **Gap 2:** new `scripts/check-spec-version.py` mechanically enforces cross-file `spec_version`/`status` agreement across the 35-file design-spec set (the surface of runbook §36.1 step 2.4) — exactly the drift the 2.4.1 review had to fix by hand. CI (`.github/workflows/validate-catalog.yml`, renamed "Validate Design Spec") now runs both scripts as separate jobs, triggered on `design-spec/**` and `scripts/**`. Spec text updated to match: §7A.5 rewritten from "must additionally cover" to present-tense "implements" (and the stale "script lives in the GitHub repo, not the design workspace" claim corrected — both scripts live under `scripts/`); runbook §36.1 step 2.4 gained the enforcing-script reference. Both validators pass clean (catalog 0/0; stamps agree at 2.4.2). Spec content changed; **incremented 2.4.1 → 2.4.2**. |
| 2.4.1        | 2026-07-15 | §36.1 Design Readiness Review (full mode) — all 20 §34 items reset and independently re-verified Done; §33 safety rules confirmed; catalog validator passed (0 errors, 0 warnings, 15 agents, 16 domains); relationship/trigger vocabulary membership, reciprocity-by-trigger, and V14 (no dead tokens) verified by hand; §7A ↔ catalog ↔ `catalog.schema.json` ↔ agent-specs confirmed mutually consistent; §18.4/§23/§24/§27/§35.2 cross-references resolve. One mechanical issue resolved (step 2.4 failure): `spec_version` frontmatter disagreed across the set — `agent-catalog.yaml` was at 2.4.0 and the 15 `agent-specs/*` (profile.md + interviews.md) plus `aos-interviews.md` were at 2.3.3, having never been carried forward through the 2.4.0/2.4.1 cycles. **Reconciled all 32 design-spec files to a single `spec_version` 2.4.1** (`catalog_version` unchanged at 1.3.0). Documentation stamp catch-up only — no spec content changed; logged at 2.4.1, **no increment** (per user instruction; matches the 2.3.2 reconciliation precedent). |
| 2.4.1        | 2026-07-15 | Runbook §36.1 Design Readiness Review workflow overhauled for comprehensiveness and efficiency. Scope widened from the main spec alone to the **full design-spec document set** (spec, runbook, revision history, `agent-catalog.yaml`, `catalog.schema.json`, `agent-specs/*`, `aos-interviews.md`). Added a **mechanical-checks step** (new step 2, implementing the applicable §27 rules: schema validation, roster↔catalog↔agent-specs parity, §7A.6/§7A.7 vocabulary membership, frontmatter `spec_version`/`status` agreement, cross-reference resolution). Completeness, safety, and consistency findings now feed **one unified issue list** that gates finalization (previously only inconsistencies gated). The clean-review revision-history entry moved behind the finalization `Proceed` gate. Finalization now explicitly **restores `status` to `design_ready_for_generation`**, and §36.2 step 1 gained a matching status precondition. Fixed the broken repeat rule ("repeat steps 1 and 2" → repeat verification steps 2–5 until a clean pass, without re-entering step 1). Added a **full vs. incremental review mode** (incremental re-verifies only checklist items/sections changed since the last completed review's baseline `spec_version`; mechanical checks always run in full). Runbook frontmatter caught up from 2.3.3. **Incremented 2.4.0 → 2.4.1**. |
| 2.4.0        | 2026-07-15 | DDD Improvement Cycle 1 — typed collaboration edges. Added a **collaboration relationship taxonomy** (`informs`, `requests`, `conforms-to`, `gated-by`; DDD context-mapping equivalents documented in new **§7A.7**) and a **controlled trigger vocabulary** (29 kebab-case event tokens derived from catalog `"on":` prose, §17 workflow triggers, and §24 escalation causes) to the Agent Catalog. §7A.3 edge schema replaced `"on": string` with required `relationship:` + `trigger:` and optional `note:`; §7A.2 controlled-vocabulary rule extended to all three vocabularies; §7A.6 gained `vocabulary.relationships`/`vocabulary.triggers`, the versioning rule, the workflow/§24 carve-out, and the reciprocity-by-trigger-token rule. All 15 `agent-catalog.yaml` entries converted to typed edges (43 edges; Inbox combined escalation split per D9; global escalation rule kept as catalog-header rule per D6; `catalog_version` → 1.3.0). Validation: §7A.5 V5 extended (vocabulary membership, reciprocity by trigger token), new **V14** (no dead tokens, with carve-out), §27 updated to "V1-V8 and V14"; `catalog.schema.json` regenerated (closed enums for relationship/trigger, `on` removed) and the catalog validated against it. Downstream: §18.4 handoff `Trigger` field uses `vocabulary.triggers`; §23/§24 gained cross-references (approved decisions unchanged); §7.4 confirmed unaffected. `scripts/validate-catalog.py` update recorded as a requirement in §7A.5 (script lives in the GitHub repo). Additive schema change; **incremented 2.3.3 → 2.4.0**. |
| 2.3.3        | 2026-07-14 | §36.3 Claude Plugin Generation — refreshed the canonical `claude-plugin/aos-factory/` to spec_version 2.3.3 (38 files overwritten, none created/deleted). Bumped `.claude-plugin/plugin.json` version 2.3.2 → 2.3.3; **added the missing `## Open the AOS User Guide` (step 12) section to `skills/build-aos/SKILL.md`** (the packaged skill predated step 12); re-copied the rendered design artifacts (`agent-catalog.yaml`, `aos-interviews.md`, 15 `agent-specs/` folders) and `builder-changelog.md` byte-identical from the factory root; restamped `templates/CLAUDE.md`, `templates/AGENTS.md`, and `README.md` to 2.3.3. Also fixed a **pre-existing malformed `plugin.json`** (missing comma after `"name"`, which made the manifest invalid JSON) and a stale/typo'd description string. §36.3 step 8 validation passed (manifest well-formed, both builders map to SKILL.md with name+description, rendered artifacts byte-identical to factory root). Manual steps (zip, local-load test, marketplace publish) remain outstanding per §28.2. Logged at 2.3.3, no increment. |
| 2.3.3        | 2026-07-14 | §36.2 AOS Factory Generation — regenerated the framework at spec_version 2.3.3 (§35.2 scope, 36 files under `aos-factory/`). §34 checklist all Done, §33 safety rules satisfied, catalog validator passed (0 errors, 0 warnings, 15 agents, 16 domains). Advanced the `spec_version` stamp on all 36 framework files 2.3.2 → 2.3.3 and re-rendered the factory-root copies of `agent-catalog.yaml`, `aos-interviews.md`, and all 15 `agent-specs/` folders byte-identical to their design-spec sources (source companion stamps also bumped 2.3.2 → 2.3.3 to preserve source↔copy byte-identity; content unchanged). `builder-changelog.md` gained a 2.3.3 row. No change to builder logic or agent content vs. 2.3.2. spec_version already at 2.3.3 from the §36.1 cycle; logged here at 2.3.3, no further increment. Follow-up (separate §36.3): regenerate the plugin so `claude-plugin/aos-factory/` catches up to 2.3.3. |
| 2.3.3        | 2026-07-14 | §36.1 Design Readiness Review — all 20 §34 items reset, independently re-verified, and re-marked Done; §33 safety rules confirmed; catalog validator passed (0 errors, 0 warnings, 15 agents, 16 domains); all 15 agent-specs folders (profile.md + interviews.md), agent-catalog.yaml, and aos-interviews.md confirmed present. This cycle formally **adopts the new §9.3 step 12 "Open the AOS User Guide"** (added to the setup sequence and reflected in `build-aos.md`), and **resolves one consistency item (C1):** `build-aos.md` carried an `## Open the AOS User Guide` section absent from the §12.1 builder schema — §12.1's schema block and prose were aligned to list it, restoring §1.6.1 spec↔artifact parity. Spec changed; **incremented 2.3.2 → 2.3.3**. Follow-up (separate §36.2): regenerate the factory/plugin so their `spec_version` stamps catch up from 2.3.2 to 2.3.3 — file *content* already matches. |
| 2.3.2        | 2026-07-13 | §36.3 Claude Plugin Generation — packaged the 2.3.2 framework into `claude-plugin/aos-factory/` (39 files, all newly created, no overwrites): authored `.claude-plugin/plugin.json` (version 2.3.2, synced to spec_version), `skills/build-aos/SKILL.md` and `skills/build-agent/SKILL.md` (builder bodies + invocation frontmatter), `templates/CLAUDE.md`, `templates/AGENTS.md`, and `README.md`; copied byte-identical `agent-catalog.yaml`, `aos-interviews.md`, `builder-changelog.md`, and all 15 `agent-specs/` folders from the factory root. §36.3 step 8 validation passed (manifest well-formed, both builders map to SKILL.md with name+description, rendered artifacts byte-identical to factory root). Framework unchanged since packaging; held at 2.3.2, no increment. |
| 2.3.2        | 2026-07-13 | §36.2 AOS Factory Generation — generated the factory framework from spec_version 2.3.2 (§35.2 scope, 36 files under `aos-factory/`): authored `/build-aos.md`, `/builders/build-aos.md`, `/builders/build-agent.md`, `/builder-changelog.md`; rendered byte-identical factory-root copies of `/agent-catalog.yaml`, `/aos-interviews.md`, and all 15 `/agent-specs/[agent-name]-agent/` folders (profile.md + interviews.md). §34 checklist all Done, §33 safety rules satisfied, catalog validator passed (0 errors, 0 warnings, 15 agents, 16 domains). Spec unchanged; held at 2.3.2, no increment. |
| 2.3.2        | 2026-07-13 | §36.1 Design Readiness Review — all 20 §34 items reset, independently re-verified, and re-marked Done; §33 safety rules confirmed; catalog validator passed (0 errors, 0 warnings, 15 agents, 16 domains); all 15 agent-specs folders (profile.md + interviews.md), agent-catalog.yaml, and aos-interviews.md confirmed present. Canonical design (Sections 1–32) and runbook reviewed for logical consistency; no functionality-impacting inconsistencies found. Held at 2.3.2, no increment. |
| 2.3.2        | 2026-07-13 | Revision-history reconciliation (Design Readiness Review, option A) — frontmatter and table caught up from 2.2.0 to the committed spec_version 2.3.2. Consolidated back-fill of previously unlogged cycles: **2.2.1** shipped `templates/AGENTS.md` with the plugin (build-aos provisions `/AGENTS.md` alongside `/CLAUDE.md` non-destructively); **2.2.2** shipped the rendered design artifacts (`agent-catalog.yaml`, `agent-specs/`, `aos-interviews.md`) with the plugin (§28.2 layout + packaging step 2; runbook §36.3 sub-step 3.3 + step 8 byte-identity validation); **2.2.3** re-shipped the rendered artifacts and regenerated the factory/plugin. The 2.2.3→2.3.2 increments were carried through release/2.0 branch merges without separately logged per-version narratives and are recorded here as a reconciliation. Documentation catch-up only; no change to the specification, no increment. |
| 2.2.0        | 2026-07-12 | §36.1 Design Readiness Review — all 20 §34 items reset, independently re-verified, and re-marked Done; §33 safety rules confirmed; catalog validator passed (0 errors, 0 warnings, 15 agents, 16 domains). Full spec (Sections 1–32) and runbook reviewed for logical consistency; no functionality-impacting inconsistencies found. Held at 2.2.0, no increment. Cosmetic note only: §1.6.10 repo URL (`Open-AOS-Factory`) vs local folder name (`OpenAOS`) — no action. |
| 2.2.0        | 2026-07-10 | §36.1 Design Readiness Review (second cycle) — all 20 §34 items reset, independently re-verified, and re-marked Done; §33 safety rules confirmed; catalog validator passed (0 errors, 0 warnings, 15 agents, 16 domains); all companion source artifacts (agent-catalog.yaml, 15 agent-specs folders, aos-interviews.md, catalog.schema.json) confirmed present. No functionality-impacting inconsistencies found. Held at 2.2.0, no increment. |
| 2.2.0        | 2026-07-10 | §36.1 Design Readiness Review — all 20 §34 items reset, independently re-verified (`scripts/validate-catalog.py`: 0 errors, 0 warnings, 15 agents), and re-marked Done; §33 safety rules confirmed. No functionality-impacting inconsistencies found in the canonical design or runbook; verified the v2.2.0 single-instance collapse (removal of `/aos-router.md` and the `aos_router` file type) fully purged from both documents. Held at 2.2.0, no increment. Follow-up noted outside this workflow's scope: `AGENTS.md`, `README.md`, and `claude-plugin/aos-factory/README.md` still reference the removed router mechanism — deferred to a separate task. |
| 2.2.0        | 2026-07-10 | Single-Instance, Minimal Routing (Phase 3 of v2.0): collapsed the multi-instance routing machinery now that a workspace hosts the factory plus exactly one AOS instance. §16.10 Router File Schema deleted and `/aos-router.md` + the `aos_router` file_type eliminated entirely; the factory-vs-instance guard folds into §16.10 (renumbered) Workspace-Root CLAUDE.md Schema, with §16.11 (was §16.12) Global Permissions Seed following. §1.6.6 renamed "Single Instance, Minimal Routing"; §1.6.8, §1.6.11/G5 reworded to drop routing-across-instances language. §4.1 and the §1.1 overview tree reduced to a single AOS Workspace root file (`/CLAUDE.md`) and one sibling AOS instance. §7.4, §10.3, §9.3, §12.1, §17.1, §28.1, §28.2 updated to remove router provisioning, joint router ownership, call-name/signal-match resolution, session pins, and cross-instance branching. Cross-agent routing (Chief of Staff orchestration, designated-owner routing, learnings attribution) is unaffected. |
| 2.1.1        | 2026-07-08 | §36.1 Design Readiness Review cycle — all 20 §34 items re-verified, §33 safety rules confirmed, catalog validation passed (15 agents, 0 errors); two functionality-impacting inconsistencies resolved: §4.1 instance tree gained the missing `/outputs` subtree, §16.6 guide-skeleton metadata line gained the missing `spec_version` stamp; runbook §36.1 step 1.2 editorial fix |
| 2.1.0        | 2026-07-06 | Lifecycle Profiles, Agent Advertising, Feedback Agent, Call-Name Routing + Schema Explicitness (Phase 2 of v2.0): §7B profiles gain five lifecycle sections; new §7C scripted interviews (H20); 14 per-agent builders collapsed into the generic build engine `build-agent.md`; `agent-profiles/` reorganized into per-agent `agent-specs/` folders; Feedback Agent added as fifth governance agent (`feedback.upstream`); F2 review-time advertising; F4 call-name routing (§16.10 router schema); BF-001 routing + attribution rules; U1 explicit schemas (§16.7–16.12 and in-place upgrades, items A1–H20); U2 `catalog.schema.json` + repo CI; U3 `/outputs` folder |
| 2.0.0        | 2026-07-01 | Canonical plugin directory aligned to the repo — §36.3 default plugin target corrected from `plugin/aos-factory/` to `claude-plugin/aos-factory/`, the path `.claude-plugin/marketplace.json` actually publishes; runbook §36.3 steps 2 and 4 updated (historical 1.0.4 entries mentioning the old path left unchanged for fidelity). Held at 2.0.0, no increment. |
| 2.0.0        | 2026-07-01 | Factory self-containment and workspace-root provisioning — factory now renders root copies of `agent-catalog.yaml` and `agent-profiles/[agent-name]-agent.md` (runbook §35.1/§35.2, §28.1); AOS instance build now provisions workspace-root `/aos-router.md` and `/CLAUDE.md` from shipped example copies, non-destructively (§4.1, §9.3, §12.1, §28.2). Held at 2.0.0, no increment. |
| 2.0.0        | 2026-07-01 | §36.1 Design Readiness Review — all 20 §34 items reset, independently re-verified, and re-marked Done; §33 safety rules confirmed. One functionality-impacting inconsistency found and resolved: runbook §35.2 generation scope had drifted from the canonical 14-agent roster — corrected to `build-aos` plus the 14 canonical agent builders; runbook frontmatter caught up to spec_version 2.0.0. Folded into 2.0.0 as completion of the roster consolidation, no increment. |
| 2.0.0        | 2026-06-30 | Agent Catalog + Agent Profiles (Phase 1 of v2.0): new §7A (machine-checked agent identity/ownership catalog) and §7B (per-agent behavioral profiles), both as source under design-spec/; `agent-catalog.yaml` + 14 profiles authored for the 14-agent §7.3 roster; §7.4 demoted to a generated view; §11/§12 made catalog+profile projections; §10.3.1 instance-scope overlap check; §7.5 Phase A/Phase B split; §14.8/§15.4/§27 updated for the new `agent_catalog` and `agent_profile` file types |
| 1.0.5        | 2026-06-11 | §36.1 Design Readiness Review — all §34.1 (20) and §34.2 (5) items reset, independently re-verified, and re-marked Done; canonical design (Sections 1–32) reviewed for logical consistency. No functionality-impacting inconsistencies found, no increment. Non-blocking observation dispositioned "leave as-is": §27's agent-completeness list does not name the per-agent Build Summary (§13/§15.4), still produced via the §12 Handoff Summary step. |
| 1.0.5        | 2026-06-11 | Version model simplified: `builder_version`/`schema_version` collapsed into `spec_version`; `aos_version` assignment/increment rule + Review-Agent ownership added; definition/data file definitions + drift invariant added; AOS User Guide reclassified as a regenerable projection |
| 1.0.4        | 2026-06-11 | §36.3 Claude Plugin Generation workflow authored; §28.2 aligned to the shipped plugin (examples/ → templates/, README.md added) |
| 1.0.3        | 2026-06-11 | §36.1 Design Readiness Review cycle — checklists verified; §34.2 item 1 dispositioned N/A; §28.1 step 3 citation corrected; no functionality-impacting inconsistencies found |
| 1.0.2        | 2026-06-10 | Document restructure — light split into the 3-file set       |
| 1.0.1        | 2026-06-10 | Design consistency resolution cycle — 16 items, surfaced across 5 re-read iterations of the §36.1 loop |
| 1.0.0        | 2026-06-02 | Baseline design accepted; initial consistency resolution (4 items) |

## 2.1.1 — Design Readiness Review consistency fixes (2026-07-08)

A full §36.1 Design Readiness Review cycle against spec_version 2.1.0, implemented as one consolidated increment. All 20 §34 checklist items were reset, independently re-verified, and re-marked Done; the §33 safety-related governance rules were confirmed satisfied; the catalog validator (`scripts/validate-catalog.py`) passed with 0 errors and 0 warnings across 15 agents and 16 vocabulary domains, and all companion source artifacts (agent-catalog.yaml, 15 agent-specs folders, aos-interviews.md) were confirmed present. Two functionality-impacting inconsistencies were found and resolved, plus one editorial fix:

1. **§4.1 instance tree omitted `/outputs`.** The canonical §4 top-level tree includes `/outputs/[agent-name]-agent/` (relied on by §5.1, §14.8, §29, §30), but §4.1's framework-vs-instance layout showed the `/[aos-name]/` tree without it, so the two normative trees disagreed. Resolution: `/outputs` (with its per-agent subfolder line) added to the §4.1 instance tree.
2. **§16.6 guide-skeleton metadata omitted `spec_version`.** The skeleton's header-metadata line listed only file_type, AOS name, aos_version, and last_updated, contradicting the §16.6 prose and the mandatory §15.1 stamping rule. Resolution: `spec_version` added to the skeleton metadata line.
3. **Runbook editorial fix.** §36.1 step 1.2 corrected from "(Replace [ ] with [ ])" to "(Replace [x] with [ ])".

## 2.1.0 — Lifecycle Profiles, Agent Advertising, Feedback Agent, Call-Name Routing, Schema Explicitness (Phase 2 of v2.0) (2026-07-06)

Phase 2 of the v2.0 release (internal-only/feature-specs/v2.0-phase-2-agent-improvements.md; approved 2026-07-05, amended through 2026-07-06), implemented as one consolidated increment covering seven items (F1–F4, U1–U3) plus the two BF-001 generalized rules. This is an internal milestone: no plugin is published at 2.1.0. Open questions resolved with the user on 2026-07-06: **O2** — the 14 per-agent builder files are removed entirely (no thin wrappers); **O5** — call name is opt-in at instance build with a suggested default; **O6** — call-name collisions are rejected with alternatives suggested; **O7** — dog names are the suggested default framing, any single-word name accepted (guidance against human-sounding names); **O8** — no call name is reserved for unbuilt instances; **O9** — interview `ask` wording may be paraphrased (content, order, and captured fields fixed). Deferred with notes: **O3** email transport (submissions queue as `staged`; user prompted to send manually; connector choice lands with Phase 3 portability); **U1-C11** seeded section lists for the global memory files; the optional U2 extension of JSON Schema validation to §15 frontmatter.

1. **F1 — Full-lifecycle agent profiles.** §7B.3 reorganized into five normative lifecycle sections (Initialization, Operation, Usage-Driven Evolution, Update, Retirement); evolution is data-layer only, strengthening the §14.8 drift invariant in effect (§14.8 semantics unedited); Update sections carry MIGRATION INSTRUCTIONS executed by the factory's update flow; retirement gains usage signals surfaced at quarterly review. All 15 profiles conform; governance profiles state the no-retirement rule. New validation checks V11–V13 (§7B.5, §27).

2. **U1-H20 — New §7C "Agent Interviews".** Scripted interviews (`file_type: interview_script`, 8-field question-entry schema) as the third per-agent design artifact; §7.5/§7B.5 Phase A extends to three artifacts. The §9.1 move-faster exception is now deterministic (skippable questions resolve to defaults). §26 restated as script properties.

3. **Generic build engine (O2).** §8/§12 restructured: `/builders` = `build-aos.md` + `build-agent.md`; the engine instantiates any catalog entry + profile + interviews into the §5.1 file set. §9.3/§9.4/§10.1/§10.3/§14/§28/runbook §35 references re-pointed; registry and map carry no per-agent builder column (MF2 resolved by O2).

4. **agent-specs/ reorganization (H20).** `design-spec/agent-profiles/[slug].md` → `design-spec/agent-specs/[slug]/profile.md` + new `interviews.md` per agent (14 authored from the former builders' interview content, §26 properties preserved); `aos-interviews.md` authored at the design-spec root from build-aos's discovery questions, including the F4 call-name entry. Document Set listing, §4.1, §7A.4, §7B.2, §14.8 path mention, §15.4 rows, §28.1, and runbook §35 updated (MF6 resolved — F1 and H20 authored together).

5. **F3 — Feedback Agent.** Fifth governance agent (roster edits: §1.6.2, §2.3, §7.1, §7.3, §7.4, AGENTS.md); new governance domain token `feedback.upstream` (§7A.6); catalog entry + reciprocal review-agent edge (catalog_version 1.1.0); §22 matrix row; §17.4/§17.5 self-examination steps; §6 gains `/logs/feedback-log.md` with the U1-B8 entry schema as new §16.7 (MF1 previously resolved; MF3 resolved — A4 wraps the same table). The "escalates-to user" edge is expressed as an approval_required_action (edges must resolve to agent slugs per V5). New `agent-specs/feedback-agent/` profile + interviews. `feedback_log` added to §15.4.

6. **F2 — Advertising uninstalled agents.** Rhythm-bound suggestion-only surfacing: §10.4 (map as data source), §16.6 (Available Agents subsection under Managing Agents), §17.3/§17.5 (advertising checks), normative anti-nagging rule; CoS and Review profile Operation sections updated.

7. **F4 + U1-F14 — Call-name routing and router schema (authored together; MF5 resolved).** New §16.10 router file schema: Resolution Order with the call-name match as the explicit-override tier subject to the framework-vs-instance carve-out; Registered Targets table with Call name column; matching rules (case-insensitive, start-of-prompt, registry-only, unique per router); session-pin effect. §17.1 prose gains the tier; §28.2 examples become renderings of §16.10/§16.11.

8. **U1 — Explicit schemas.** New §16.8 (B6 change-log entries), §16.9 (B7 builder-changelog), §16.11 (F15 workspace CLAUDE.md), §16.12 (A5 global-permissions seed); in-place upgrades: §14.3 manifest example (A1), §10.4 map schema (A2), §10.3/§10.3.1 registry + absorbed-ownership block (A3), §22 file schema (A4), §16.2/§20.3 memory entry block (C8), §6.1 learnings index + entry format (C9/C10 — C10 carries the BF-001 attribution rule: learnings are filed under the process-owning agent, recorded in Applies To), §18 template body skeletons (D12), §21 project-file schemas (E13), §9.1 pre-build preview block (G16), §9.3 `/logs/aos-build-summary.md` (G17), §28 upgrade-recommendation block (G18), §17.1 startup-brief skeleton (G19). Numbering note: B8 landed first as §16.7 (per the feature spec's implementation order), so B6/B7 took §16.8/§16.9 rather than the sketch's pencilled §16.7.

9. **BF-001 routing rule (rides F1).** §2.2 gains the designated-owner routing language ("workflow owner"); the CoS profile's Operation section carries the rule with the review workflows named as the motivating instance.

10. **U2 — Schema format decision.** Markdown skeletons remain normative; derived `design-spec/catalog.schema.json` + `scripts/validate-catalog.py` + `.github/workflows/validate-catalog.yml` enforce the mechanical surface of V1–V4 and V5's slug half on catalog PRs (noted in §7A.5). Validated clean: 0 errors, 15 agents, 16 domains.

11. **U3 — /outputs folder (MF4 resolved).** §4 tree gains root-level `/outputs` with one subfolder per installed agent; boundary rule with `/projects` (standalone deliverables only); cross-agent lineage via handoff summary; §5.1 creates the subfolder at agent build; every catalog entry's artifacts_owned gains its `/outputs/[slug]/**` glob (catalog_version 1.2.0); §14.8's data-file examples line now includes `/outputs` — **§14.8 thereby moves from Untouched to Extended** (example line + agent-specs path fix only; the drift invariant and file-kind definitions are unchanged); §29 naming rule; §30 archive-policy lines.

Not in scope at this milestone: factory regeneration (the `aos-factory/` rendered tree remains at 2.0.0 pending a §36.2 generation run) and plugin packaging/publication (deferred to Phase 3 close per O1).

## 2.0.0 — Agent Catalog and Agent Profiles (Phase 1 of v2.0) (2026-06-30)

Phase 1 of the v2.0 release (internal-only/v2.0-phased-plan.md): moved each agent's identity and ownership out of prose buried in `build-*-agent.md` builders into a structured, machine-checkable, framework-level catalog, and each agent's behavioral narrative into a per-agent profile. Single-responsibility and non-overlap are now verified, not merely asserted. Five open design decisions (O1–O5, from the catalog proposal / GitHub issue #4) were resolved with the user, all accepting the recommended default: pre_authorized_actions stays in the catalog (O1); the controlled-vocabulary rule stays in the spec while the vocabulary list lives in the catalog (O2); §7.4 is demoted to a generated view (O3); the instance-scope overlap check is specified in §10.3 (O4); agent creation is split into a Phase A design step and a Phase B build step (O5). The roster was held at the 14-agent §7.3 set (Tutor and Document retained; no Finance or Health/Life-Logistics agent). The catalog and profiles are versioned as **source under design-spec/**, with rendered copies shipped in the factory.

1. **New §7A "Agent Catalog" section** — defines the catalog's intent and three-layer model (spec = rules, catalog = data, §11 instruction file = projection), the normative controlled-vocabulary rule, the per-agent entry schema, the catalog file's source location/format/ownership (`design-spec/agent-catalog.yaml`, YAML, framework-owned, with a rendered copy at the AOS Factory root), and the Review Agent's eight validation procedures (V1–V8).

2. **New §7B "Agent Profiles" section** — defines a per-agent narrative file (`design-spec/agent-profiles/[agent-name]-agent.md`) that holds behavior only and references the catalog for identity; its schema, its projection into §11's narrative sections, and two additional validation procedures (V9–V10). Profiles are framework-owned definition files (§14.8).

3. **`agent-catalog.yaml` authored for the full 14-agent §7.3 roster** — 4 governance + 10 productive entries with disjoint `domains_owned` drawn from the controlled vocabulary, disjoint `artifacts_owned`, reciprocal `collaborates_with` edges, and derived `non_responsibilities`; validated against V1–V8.

4. **14 agent profiles authored** — one per §7.3 agent, derived from the existing builders, behavior-only.

5. **§7.4 demoted to a generated view; §2.1 points at catalog validation** (§7A.5, §27) as the SRP enforcement surface (O3).

6. **§11 and §12 made catalog + profile projections** — §11's identity sections project from the catalog entry and its narrative sections from the profile; §12 gained rendering rules for builders to read both; §16.1 gained the `pre_authorized_actions` cross-reference note so agent configs don't restate the catalog (O1).

7. **§10.3 gains the instance-scope overlap check (new §10.3.1)** (O4); **§7.5 rewritten for the Phase A / Phase B design-vs-build split** (O5) — a design-time primitive, not a runtime Agent Maker Agent.

8. **§14.8, §15.4, §27 updated** — the catalog and profiles are classified as definition files (framework-owned, read-only in instances); the instance registry's absorbed ownership fields are classified as data (targeted, approval-gated merge only); new controlled `file_type` tokens `agent_catalog` and `agent_profile` were added to the §15.4 vocabulary and file-type-by-file table; §27 requires catalog + profile validation (V1–V10) to pass before an AOS or an agent build is considered complete.

Deferred (explicitly out of scope for this release, per user instruction): the continuous learning-capture loop is not reintroduced. Framework files (the on-disk builders and the factory's rendered catalog copy) are reconciled to this 2.0.0 spec in a separate, approval-gated rebuild; the public plugin is not re-packaged in this phase — that happens once, at the end of Phase 2 (platform portability), per the phased plan.

## 1.0.5 — Version Model Simplification and Drift Control (2026-06-11)

The versioning and update policy (§14) and the frontmatter standards (§15) were simplified, and the supporting drift-control and instance-versioning rules were made explicit. No agent roster, workflow, template, or path convention changed.

1. **`builder_version` and `schema_version` collapsed into `spec_version`** — because the factory is a pure rendering of the spec (§1.6.1), a framework change can only come from a spec change, and a file-schema change is itself a spec change. The two derived numbers carried no independent information and invited drift, so they were removed. §14.1 now states the framework version *is* the `spec_version`; §14.2 and the §15.1–15.3 frontmatter examples were updated; a mandatory stamping rule was added (every generated file records the `spec_version` it was rendered from). `compatible_aos_versions` is retained as the separate builder→instance compatibility axis.

2. **Instance versioning made explicit** — §14.3 now defines two independent instance axes: `spec_version` (provenance / last-conformed design) and `aos_version` (the instance's own evolving version). §14.3.1 adds the previously-missing assignment and increment rule (start at 1.0.0; PATCH/MINOR/MAJOR semantics, with MAJOR as the line that interacts with `compatible_aos_versions`). The `/aos-manifest.md` schema replaced "Builder Version Used / Schema Version Used" with "Spec Version." Per-file version fields are clarified as creation/regeneration provenance stamps; the live instance version lives only in the manifest.

3. **`aos_version` ownership assigned** — the Review Agent owns and reconciles `aos_version` (monthly reconciliation against `/logs/change-log.md`, plus verification in its completeness audit); the triggering event is logged by the agent that made the change (Chief of Staff coordinating); breaking/MAJOR bumps are applied at the time of change. Recorded in §7.4 and §14.3.1.

4. **Definition/data file definitions and the drift invariant added** — new §14.8 defines a *definition file* (fully determined by spec + instance configuration; factory-owned; regenerable) and a *data file* (accumulates from operation/user input; agent-owned; never overwritten by the factory), with an operational test for each and a rule for mixed/derived files. The invariant: agents write data files, the factory owns definition files, and no operating agent modifies a framework-derived definition file. This bounds drift to data and keeps factory updates a clean overwrite rather than a three-way merge.

5. **AOS User Guide reclassified as a regenerable projection** — §16.6 and §17.4 now describe the guide as a projection (§14.8): a regenerable view assembled from definition files plus its embedded Change Log as the sole data input. The Review Agent regenerates it rather than hand-editing prose, so a newer factory can regenerate it wholesale without a merge.

6. **Compatibility-note and plugin-sync wording updated** — §14.6 reframes compatibility detection around `spec_version` (a coarser signal: warn on mismatch, block only when explicitly broken). §28.2 and runbook §36.3 now sync the plugin version to `spec_version`. §14.5 adds a note distinguishing `/builder-changelog.md` (framework/packaging changes, ships in the plugin) from this revision history (design audit trail), now that both are numbered by `spec_version`.

Deferred (noted, not yet authored): the factory→instance migration/reconciliation workflow that a MAJOR `aos_version` bump implies (§14.4, §28 hold its current scope).

## 1.0.4 — Plugin Generation Workflow (2026-06-11)

The §36.3 Claude Plugin Generation workflow was authored in the generation
runbook, completing the third of the three gated build workflows in §36
(alongside §36.1 Design Readiness Review and §36.2 AOS Factory Generation).

1. **§36.3 workflow authored** — the `Insert workflow here.` placeholder was
   replaced with an eight-step, `Proceed`-gated workflow. It packages the
   already-generated framework files (§35.2 output) into the Claude plugin
   layout of §28.2: a precondition check that the framework exists, a dry-run
   preview, the global file-safety/overwrite-approval model when refreshing the
   canonical `plugin/aos-factory/`, the exact-string `Proceed` gate (scoped to
   writing the plugin directory only — not zipping, local-load testing, or
   marketplace publishing), and a closing validation step against §§27–28.2 and
   §34. Builders map to `skills/build-*/SKILL.md`; the root `/build-aos.md`
   entry pointer is not packaged separately.
2. **§28.2 aligned to the shipped plugin** — the canonical plugin layout and
   packaging steps were reconciled with the real `plugin/aos-factory/`: the
   shipped example workspace-root files now live under `templates/` (was
   `examples/`), and `README.md` (plugin install + usage instructions) was added
   to the layout diagram and packaging step 3. The `CLAUDE.md` filename is
   unchanged (already uppercase per 1.0.1 item 12). The plugin `README.md` copy
   instructions were corrected to `CLAUDE.md` to match.

## 1.0.3 — Design Readiness Review (2026-06-11)

A §36.1 Design Readiness Review was run end to end. The cycle surfaced no inconsistencies that impact the functionality of the generated factory; the design remained internally consistent across path conventions (§4.1), the file_type/status vocabularies (§15.4–15.5), the agent roster and builder set (§7–§8, §35.2), workflow/template ownership (§17–§18), and the inbox-move exception (§3.2/17.6/31). The items below record the cycle's dispositions.

1. **§34.1 Design Completion Checklist** — all 20 items re-verified against current spec content and confirmed Done. Cross-checks reconciled: 4 required + 12 optional agents = 16, matching the 16 agent builders (§8.2) and the §35.2 generation scope; 9 global workflows (§17) and 6 templates (§18) match the §6 global-files list.
2. **§34.2 Safety Confirmation Checklist** — items 2–6 verified Done. Item 1 ("No builder files have been generated yet") is no longer literally true: the factory builders already exist under `aos-factory/`. Per user decision, item 1 is dispositioned **N/A** for the post-generation state, with its wording left unchanged; the operative safety property (no overwrite without a separate `Proceed`) remains enforced by §34.2 items 3 and 5 and by §3.2. Consequence noted: a §36.2 generation run will halt at its step-2 check on item 1 and notify the user, which is acceptable safe behavior.
3. **§28.1 step 3 citation corrected** — step 3 previously called `Proceed` "the safety gate in Section 34.2"; the exact-string approval gate is defined in §3.1, while §34.2 is the Safety Confirmation Checklist. Step 3 now reads "the approval gate in Section 3.1; the §34.2 Safety Confirmation Checklist must also be satisfied." This corrects user-facing generation documentation only; no normative behavior changed.

## 1.0.2 — Document Restructure (2026-06-10, Light Split)

The specification was split into a small companion set for readability, with no change to normative design content:

1. **Revision history extracted** — the dated Design Consistency Resolution cycles (2026-06-02 and 2026-06-10) were moved verbatim from the specification's front matter into this file.
2. **Generation runbook extracted** — Sections 33-37 (final consolidation decisions, ready-to-generate checklist, proposed builder generation scope, required continuation behavior, and status) were moved verbatim to aos-factory-generation-runbook.md. Section numbering 33-37 is preserved so existing cross-references continue to resolve.
3. **Document Set pointer added** — the canonical specification now lists the companion files and reaffirms it remains the single source of truth (Section 1.6.1). The `Proceed` safety gate is unchanged.

No Section 1-32 design content was altered.

## 1.0.1 — Design Consistency Resolutions (2026-06-10)

The final consistency check ran as the §36.1 loop — re-reading the spec until no further issues surfaced. It took five successive re-read iterations, which collectively resolved the sixteen items below. They are recorded here as the single resolution cycle they constitute (1.0.0 baseline → 1.0.1). Items are in the order resolved.

1. **Workspace layout model** — Section 4.1 rewritten to the AOS Workspace model: the AOS Workspace root holds /aos-router.md and /claude.md, with the AOS Factory (aos-factory/) and each generated AOS instance (/[aos-name]/) as sibling folders. Reconciled with Sections 7.4, 10.3, 15.4, and 28.1. A three-term definition (AOS Workspace, AOS Factory, AOS) was added to Section 1.1.
2. **Hard-coded instance names** — "AOS-03" renamed to "AOS Workspace" throughout (Sections 7.4, 10.3, 15.4, 28.1). The hard-coded work-aos/personal-aos instance names in Sections 7.4 and 10.3 were generalized to "every AOS instance's Chief of Staff Agent," matching the instance-agnostic design (1.6.6).
3. **Example workspace-root files** — Sections 28.1 and 28.2 now state that examples/aos-router.md and examples/claude.md are authored during plugin packaging (28.2, step 3), not during framework generation (Section 35), closing the previously undefined origin of these files.
4. **Workflow ownership (first set)** — "Primary owner" lines added to Section 17 for consistency with 1.6.7 and the existing 17.3/17.9 entries: Chief of Staff Agent for 17.1 (daily startup) and 17.2 (end-of-day); Review Agent for 17.4 (monthly) and 17.5 (quarterly).
5. **Self-referential filename** — Section 36.1 and the frontmatter title updated to match the actual filename (aos-factory-design-specification.md), and "- Final" dropped from the title.
6. **Factory-internal path notation** — Section 4.1's tree nests build-aos.md, builder-changelog.md, and builders/ inside /aos-factory/, but Sections 6, 8.2, 8.3, 9.3, 10.3, 12.1, 14.5, 28.1, 28.2, and 35 wrote them with workspace-root leading slashes. Section 4.1 now adds a factory-relative path convention (parallel to the instance-relative rule): factory-internal paths resolve against the AOS Factory root (/aos-factory/), so a leading slash denotes the root of the relevant container, not the AOS Workspace root. Existing path text is unchanged.
7. **Second exact-string command** — Section 36.1's suggested prompt used a second exact-string gate, "Continue," conflicting with the single-exact-string-command principle (1.6.8, 16.6). The continuation is now split into two sequentially gated workflows (Final Design Consistency Check; Framework Generation), each gated by an action-specific "Proceed" per Section 2.5. "Proceed" remains the only exact-string command and the review gate no longer auto-triggers generation.
8. **Incomplete workflow ownership** — "Primary owner" lines added to the three remaining Section 17 workflows that lacked them, owned by required agents: Chief of Staff Agent for 17.6 (inbox-to-task, with Inbox / Communications Agent support when installed) and 17.7 (project kickoff, with Project Manager Agent support when installed); Chief of Staff Agent with Memory Agent support for 17.8 (decision capture). Every Section 17 subsection now carries a Primary owner line.
9. **Competing end-of-build artifacts** — Section 13 defined a "Build Summary" produced at the end of each agent build, while Section 18.4's handoff-summary-template listed "when an agent completes a build" among its uses, and Section 12's builder schema ends with a "## Handoff Summary" step. The scopes are now separated: the Section 13 Build Summary is the canonical end-of-build artifact; Section 18.4's handoff-summary-template scope is narrowed to cross-agent handoffs, escalations, and major-work transfers (build completion removed); and Section 13 now cross-references that the builder's "## Handoff Summary" step (Section 12) emits the Build Summary. Section 23 (cross-agent handoffs use the template) is unchanged and now non-overlapping.
10. **Root files absent from folder trees** — Section 6 creates /aos-manifest.md and /aos-map.md at the instance root, but the Section 4 and 4.1 trees showed only folders, whereas Section 5's tree shows the agent's .md file. Both instance-root trees now list /aos-manifest.md and /aos-map.md, matching Section 5's convention.
11. **Generic builder frontmatter file_type** — Section 15.1's example uses file_type: agent_builder, while build-aos.md is aos_builder and the root /build-aos.md is builder_entry. Reviewed and left as-is: Section 15.1 is explicitly illustrative ("like this") and the per-file file_type values are already specified in Sections 12.1 and 15.4. No change.
12. **Project-instructions filename casing** — the workspace-root project-instructions file was written as lowercase /claude.md (Sections 1.1, 4.1, 15.4, 28.1, 28.2), but Claude Cowork / Claude Code auto-loads CLAUDE.md (uppercase), which is also the actual file in this workspace. All normative references were renamed /claude.md → /CLAUDE.md, including the shipped example (examples/CLAUDE.md in 28.2). The earlier dated entry (2026-06-02) retains the original lowercase for historical fidelity.
13. **Required-agent display names** — the full names "Security / Permissions Agent" and "Review / Reflection Agent" were shortened to "Security Agent" and "Review Agent" throughout (Sections 1.6.2, 1.6.7, 2.3, 7.1, 7.4, 16.6, 17, 22, 23, 24, 27), making the existing agent-registry example (Section 10.3) canonical. Slugs (security-agent, review-agent) and builder filenames were already short and are unchanged.
14. **Build Summary file_type** — the Build Summary (Section 13), previously the canonical end-of-build artifact with no entry in the controlled file_type vocabulary, is now a saved file with file_type build_summary (added to Section 15.4), written to /agents/[agent-name]-agent/logs/[agent-name]-build-summary.md.
15. **Design-spec frontmatter vocabulary** — file_type design_spec was added to the Section 15.4 vocabulary to cover this source document, and a Section 15.5 note records that the design spec's status field tracks design-phase lifecycle and is exempt from the four controlled artifact-status values.
16. **"build summary" naming collision** — the term "build summary" named two distinct artifacts at opposite ends of the build lifecycle. Section 9.1 step 5 ("Generate a build summary") is a pre-Proceed preview shown before any files are created, while Section 13's "Build Summary" is the canonical post-build saved artifact (file_type build_summary), emitted by the builder's final "## Handoff Summary" step and recording the files created. Because they shared the name, a builder generated from this spec could conflate them — emitting only the preview and never saving the Section 13 file, or saving it prematurely with an empty "Files Created" section. Resolution (Option A): Section 9.1 step 5 is renamed "Generate a build plan / pre-build preview"; a clarifying note distinguishing it from the Section 13 Build Summary is added to Section 9.1; and a cross-reference reserving the term "Build Summary" for the post-build saved artifact only is added to Section 13. The Section 27 build_summary validation question was left out of scope at the user's instruction.

## Design Consistency Resolutions (2026-06-02)

The following design inconsistencies were identified during the final consistency check and resolved with the user before generation planning continued:

1. **Framework vs. instance layout** — The reusable builder framework and the AOS instances it produces are sibling structures at the repository root. Instance-relative paths resolve against the target AOS instance root. See Section 4.1.

2. **Inbox move approval** — Moving files always requires explicit approval, with one narrow pre-authorized exception: moving items into /inbox/processed under the approved inbox-to-task workflow. See Sections 3.2, 17.6, and 31.

3. **Tool access source of truth** — The global tool access matrix (/configs/tool-access-matrix.md) is authoritative and overrides agent configs on conflict; agent config Tool Access sections only reference it. See Sections 22 and 16.1.

4. **Missing builder schemas** — A section schema is now defined for /builders/build-aos.md. See Section 12.1.

The remaining minor consistency items from that check, together with further consistency passes on 2026-06-03, have since been resolved and are recorded in the Design Consistency Resolutions section above; the full change history is tracked in Git.

**Note:** Many design consistency resolution loops were run prior to the implementation of this revision history log, culminating in the baseline design committed to Git.
