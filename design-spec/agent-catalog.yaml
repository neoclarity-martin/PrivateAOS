# agent-catalog.yaml — the Workflow Catalog (renamed workflow-catalog.yaml in
# Phase C). SOURCE OF TRUTH. Lives in design-spec/ and is versioned with the
# spec. Defined in design-spec/aos-factory-design-specification.md §7A.
#
# Cut to Minimal in Phase B: the 2.x fifteen-agent roster, the DDD relationship
# vocabulary, typed collaboration edges, and catalog_version are removed. The
# roster is legitimately empty until Phases D–E author the governance and
# use-case workflow definitions; validators accept an empty roster (§7A).

spec_version: 2.4.2

vocabulary:
  # workflow domain tokens (repopulated in Phases D–E)
  domains: []

agents: []
