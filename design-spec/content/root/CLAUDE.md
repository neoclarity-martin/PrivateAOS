<!-- OpenAOS workspace root file (spec §16.10). Provisioned by setup-openaos;
     if a CLAUDE.md already exists, this block is proposed as an addition. -->

# OpenAOS Workspace

This folder is an OpenAOS workspace. Standing instructions for AI agents are
in `AGENTS.md` — read it at the start of every session.

@AGENTS.md
